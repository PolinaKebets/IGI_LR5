version: '3.8'

services:
  db:
    image: postgres:14
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: chemshop_db
      POSTGRES_USER: chemshop_user
      POSTGRES_PASSWORD: chemshop_password
    ports:
      - "5432:5432"

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py seed_data &&
             gunicorn --bind 0.0.0.0:8000 chemshop.wsgi:application"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - SECRET_KEY=your-secret-key-change-in-production
      - DATABASE_URL=postgres://chemshop_user:chemshop_password@db:5432/chemshop_db
      - ALLOWED_HOSTS=localhost,127.0.0.1
      - LOG_LEVEL=DEBUG
    depends_on:
      - db

volumes:
  postgres_data:
